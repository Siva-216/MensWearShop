# ─── Local Development Only ──────────────────────────────────────────────────
# This file is NEVER committed to Git (see .gitignore).
# In production (Render), set these as Environment Variables in the Render dashboard.
# ─────────────────────────────────────────────────────────────────────────────

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://fashionworld216:fashionworld%40216@fw.yb7w6gy.mongodb.net/fashionworld?retryWrites=true&w=majority&appName=fw

# JWT signing secret (use a long, random string in production)
JWT_SECRET=fashionworld_secret_key_2024_antigravity_safe_and_long

# Gmail SMTP credentials
MAIL_USERNAME=mr.sivakumar216@gmail.com
MAIL_PASSWORD=paki njhe yman yyja

# Frontend URL — update to your deployed frontend URL in production
FRONTEND_URL=http://localhost:5173

# Server port — Render injects PORT automatically; this is only for local dev
# PORT=8082
